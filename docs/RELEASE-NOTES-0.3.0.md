# Xmouse 0.3.0

## 本版变化

- 设置页改为参考 Quicker 的左右导航结构，“常规”和“剪贴板历史”分开显示。
- 输入框、下拉框、排除列表和历史搜索/列表使用圆角外观。
- 默认搜索引擎从 Bing 调整为 Google；未修改过的旧默认配置会自动迁移。
- 新剪贴板记录默认明文保存，便于查看 `history.db` 和 `media/*.png`；仍可选择 DPAPI 加密。
- 旧 DPAPI 数据带有逐条格式标记，可与明文数据兼容读取。
- 新增“打开数据目录”按钮和专属多尺寸应用/托盘图标。
- 保持纯 Rust + Win32/GDI 实现，不引入 WebView、托管 UI 运行时或持续动画。

## 资源实测

测试环境为 Windows x64、16 个逻辑处理器，最终 Release 版启用手势（X2 触发）与明文剪贴板监听后后台空闲 60.18 秒：

- CPU 时间增量：`0.0312 s`；按 16 个逻辑处理器总容量折算约 `0.0032%`。
- 私有内存：平均 `2.18 MiB`，峰值 `2.23 MiB`。
- 工作集：平均 `14.55 MiB`，峰值 `14.57 MiB`。
- 稳定后：4 个线程、157 个句柄。

这些数据是单机空闲实测，不代表手势识别、图片解码或打开大量历史记录时的瞬时峰值。

## Edge 冲突

Edge 与 Xmouse 都可能使用“按住右键并拖动”。请打开 `edge://settings/appearance`，在“自定义浏览器”中关闭“启用鼠标手势”；或将 Xmouse 触发键改为 X1/X2。受管理设备可使用 Edge 的 `MouseGestureEnabled=0` 策略。
