# 更新日志

本文件记录 Xmouse 各版本的重要变化。版本号遵循语义化版本格式。

## [0.13.1] - 2026-07-29

### 调整

- 圆形闭合距离上限由轨迹最大跨度的 40% 调整为按横纵轴分别归一化后的 55%，允许约六分之一圈的自然重叠，同时避免椭圆比例和显示缩放改变闭合判断。
- 圆形专属相似度门槛相对全局阈值降低 0.04；其他八种手势继续使用原全局阈值和候选分差规则。
- 64 点轨迹存在非相邻线段交点且端点归一化距离不超过 80% 时，为圆形候选增加 0.05 分；仅有交点但首尾相距较远的开放轨迹不会获得加分。
- 个性化圆形样本继续与内置模板共同参与匹配，画过头的个人圆形轨迹不再被闭合预筛选提前排除。

### 质量

- 全部内置模板现在逐份执行缩放和抖动回归，不再只验证每种手势的首个模板。
- 新增顺逆时针过画圆、个性化过画圆和远端交叉开放轨迹反例测试，并确认两套 C 模板仍识别为 C。
- 39 项常规 Release 测试与 1 项真实剪贴板往返测试通过；格式检查、Release 构建和 Clippy 严格检查通过。

## [0.13.0] - 2026-07-29

### 新增

- 新增 `7` 单笔手势，默认发送 `Ctrl+Shift+Esc` 打开任务管理器。
- 新增圆形手势，默认发送 `Win+Shift+S` 打开 Windows 截图选择层；内置模板覆盖不同起点、顺逆时针、横纵椭圆和轻微未闭合轨迹，也可继续学习个人样本。
- 两种新轨迹与现有轨迹一样支持动作改绑或单独禁用；安全动作列表新增“打开任务管理器”和“打开截图工具”。

### 调整

- 左划现在切换到右侧虚拟桌面，右划切换到左侧虚拟桌面，交互按“拖动画布”方向理解；配置 schema 升级到 2，并只对仍使用旧默认映射的用户自动对调，保留自定义动作。
- 圆形识别增加闭合度预筛选，避免完整圆与 C 手势互相竞争，同时继续保留置信度和候选分差拒绝规则。

### 修复

- 深色模式下切换“全部 / 文本 / 图片”不再擦除为系统白色背景，改为无擦除同步重绘，消除按钮白闪。
- 来源应用筛选菜单在深色模式下使用主题背景、悬停色、文字和自绘勾选标记；浅色模式继续使用系统菜单。

### 质量

- 新增 schema 1→2 迁移、自定义桌面动作保留、7/C/V/L 区分、圆形起点/方向/宽高比和不完全闭合轨迹测试。
- 36 项常规 Release 测试与 1 项真实剪贴板往返测试通过；Release 构建、格式检查和 Clippy 严格检查通过。
- 50.36 秒托盘空闲测量中 CPU 增量为 0，平均私有内存 2.285 MiB，平均工作集 14.069 MiB。

## [0.12.0] - 2026-07-27

### 新增

- 七条内置轨迹现在可以分别改绑动作，也可以单独设为“不执行动作”；个人学习样本继续绑定轨迹形状，不会因动作改绑而丢失。
- 新增有限安全动作：粘贴、浏览器后退/前进、最小化、最大化/还原、显示桌面、任务视图、音量调节、静音和媒体播放/暂停。
- 绘制过程中的预测提示同时显示识别到的轨迹和当前绑定动作。

### 架构

- 新增独立 `action.rs` 动作领域模型，识别器只返回 `GestureId`，配置层负责从轨迹解析 `ActionKind`，执行层不再依赖模板命名。
- 个性化手势页布局拆分到 `ui/gesture_settings.rs`，`settings.rs` 不再维护手势页的全部控件细节。
- 旧配置中的个人样本 `action` 字段可自动迁移为新的轨迹 ID；缺少动作映射的旧配置自动使用原默认映射。

### 质量

- 新增旧配置迁移、动作改绑、重复映射拒绝和轨迹/动作解耦测试。
- 修复保存剪贴板快照时把 GDI/元文件句柄误当作全局内存读取、可能导致进程堆损坏的问题。
- 32 项常规测试和 1 项真实剪贴板往返测试通过；Release 构建和 Clippy 严格检查通过。
- 50.59 秒后台空闲测量中 CPU 增量为 0，平均私有内存 2.27 MiB，平均工作集 14.86 MiB。

## [0.11.0] - 2026-07-27

### 新增

- 剪贴板历史新增“全部 / 文本 / 图片”类型筛选，并可按来源应用组合过滤。
- 搜索词在内容标题或来源应用中以主题适配的背景色高亮，支持中文和 ASCII 大小写匹配。
- 新增默认开启的“选择后自动粘贴”：从 V 手势或托盘打开历史时，确认记录后恢复目标窗口并发送 `Ctrl+V`；从设置页打开时仍安全地只复制。

### 性能与工程

- 类型和来源条件在解密文本、读取缩略图前完成过滤，减少筛选图片或单一应用时的无效内容处理。
- 来源列表直接从 SQLite 元数据聚合，不需要加载完整历史内容，也不增加数据库版本迁移。

### 质量

- 新增类型/来源/搜索组合过滤、来源聚合、搜索高亮范围和旧配置默认值测试。
- 28 项常规测试通过，1 项真实剪贴板往返测试按需运行；Release 构建和 Clippy 严格检查通过。

## [0.10.0] - 2026-07-27

### 新增

- 新增默认开启的“全屏应用中暂停手势”，当前台目标窗口覆盖整块显示器时直接放行鼠标输入，减少 Valorant、GTA5 等全屏或无边框全屏游戏中的误触。
- 手势排除列表与剪贴板记录排除列表在配置中正式分离，避免关闭某应用的剪贴板记录时意外禁用手势。

### 性能

- 全局钩子的普通 `WM_MOUSEMOVE` 热路径在没有候选手势时直接返回，不再读取完整配置或竞争互斥锁。
- 触发按下时只读取一次必要参数；移动和松开阶段复用候选状态中的标量配置，避免高回报率鼠标持续复制个性化手势模板。

### 质量

- 新增全屏矩形、工作区最大化窗口和配置排除项独立性测试；26 项常规测试通过，1 项真实剪贴板往返测试按需运行。
- Release 构建、格式检查和 Clippy 严格检查通过。

## [0.9.0] - 2026-07-26

### 新增

- 新增 `←` 和 `→` 两个方向敏感的单笔手势，分别切换到左侧、右侧 Windows 虚拟桌面。
- 桌面切换使用系统原生 `Win+Ctrl+←/→` 快捷键，不依赖未公开的虚拟桌面接口。
- 个性化手势页支持为左右桌面切换分别录制最多 3 份个人轨迹。
- 实时手势预测、常规页映射和关于页说明同步展示两个新动作。

### 质量

- 新增左右横向轨迹方向测试，确保两个桌面手势不会互相误判。
- 24 项测试通过（23 项常规测试，1 项真实剪贴板往返测试按需运行）。

## [0.8.0] - 2026-07-26

### 新增

- 设置侧边栏新增“关于”页，展示版本、技术选型、核心功能、默认手势和三步使用方式。
- 关于页展示作者 `cirrusli`、个人 GitHub 地址和 Xmouse 项目地址，并提供“访问 GitHub”按钮交给默认浏览器打开。
- Cargo 包元数据增加作者、仓库与主页地址，为 GitHub 开源发布做好准备。

### 调整

- MIT 许可证版权信息更新为 `cirrusli`。
- 关于页不显示无关的“保存设置”按钮，并完整支持浅色、深色主题。

### 质量

- 23 项测试通过（22 项常规测试，1 项真实剪贴板往返测试按需运行）；Release 构建与 Clippy 严格检查通过。

## [0.7.4] - 2026-07-26

### 修复

- 移除剪贴板历史窗口内部背景点击关闭逻辑，修复点击弹窗内容区域时窗口意外消失的问题。
- 现在仅在历史窗口真正失去激活状态（点击外部窗口或切换应用）时自动关闭；搜索框、列表、操作按钮和窗口背景点击均保持打开。

### 质量

- 增加历史窗内部根区域、搜索框和列表点击保持可见，以及外部失活后隐藏的 Win32 消息冒烟验证。

## [0.7.3] - 2026-07-26

### 修复

- 轨迹分层窗口改为透明门控：每次显示前先以全透明状态显示并重绘新轨迹，等待 DWM 提交后再显色，避免上一条手势的合成表面闪现。
- 手势结束时先把轨迹层变为透明，再清空状态并同步提交空白表面后隐藏，确保下一次显示没有旧帧可复用。
- 剪贴板历史在点击窗口外、切换到其他应用或点击弹窗自身空白区域时自动关闭；点击外部时不抢回原窗口焦点。
- 删除和清空的确认对话框作为历史窗的拥有窗口，不会触发失焦自动关闭。

### 质量

- 23 项测试通过（22 项常规测试，1 项真实剪贴板往返测试按需运行），并增加历史窗失活/空白点击 Win32 消息冒烟验证。

## [0.7.2] - 2026-07-26

### 修复

- S 手势不再保存指向系统剪贴板的活动 OLE 对象，改为在发送 `Ctrl+C` 前逐格式复制独立数据快照，修复 Codex、Chromium 和 Electron 中恢复原剪贴板持续报 `0x800401D0 (CLIPBRD_E_CANT_OPEN)` 的问题。
- 原剪贴板的 Unicode 文本、HTML 与应用自定义内存格式会一起恢复；遇到无法可靠复制的特殊句柄格式时会在改写剪贴板前安全取消。
- 恢复阶段增加更长的剪贴板占用退避窗口，并在错误中保留 Win32 状态，便于定位真正的外部占用者。

### 质量

- 自动测试增加到 23 项，其中包含文本、HTML、自定义格式和临时文本的真实剪贴板往返测试；测试结束会恢复进入测试前的剪贴板。

## [0.7.1] - 2026-07-26

### 修复

- 手势激活时一次性把延时阶段已经采集的完整轨迹交给覆盖层，不再把前半段误画成起点到当前点的直线，实时预测也从完整轨迹开始。
- 覆盖层在重新显示前同步重绘新内容，修复上一条手势轨迹偶尔闪现的问题。
- 修复归一化后的个人样本被“至少 1 像素”规则错误判为损坏，导致录制无法保存的问题。
- S 手势保存和恢复 OLE 剪贴板改为带退避的重试；失败提示与日志现在包含具体阶段和完整错误链。
- S 手势结束时无论成功与否都会清理临时忽略计数，避免影响下一次正常剪贴板记录。

### 质量

- Release 单元测试增加到 21 项，新增激活前轨迹批量传递和归一化个人样本回归测试。

## [0.7.0] - 2026-07-26

### 新增

- 侧边栏新增“个性化手势”页，可为 ↑、L、S、C、V 分别录制最多 3 份个人单笔轨迹。
- 个人样本采用 64 点归一化格式写入版本化配置；保存后最终识别和按住触发键时的实时预测都会立即更新。
- 支持清除当前动作的个人样本；内置模板始终保留并作为兜底。

### 修复

- 修复 Codex、Chromium 和 Electron 类界面中 C 可以复制、S 却过早判定“没有读取到选中文本”的问题。
- 临时复制回退改为最长等待 1.5 秒，只有剪贴板序号变化且 Unicode 文本真正可读后才继续搜索，随后恢复原剪贴板。
- S 手势的回退错误不再被静默丢弃，日志可区分复制、读取和恢复阶段。

### 工程与质量

- 新增 `ui/gesture_editor.rs`，分离个性化画布命中与绘制；样本模型和识别热更新留在 `gesture.rs`。
- Release 单元测试增加到 19 项，覆盖个人模板学习、旧配置兼容和每动作样本上限。
- `cargo check`、Release 测试、Clippy `-D warnings` 通过，并用隔离配置完成浅/深色画布和实际轨迹保存验收。
- Release 版空闲复测中 CPU 时间增量低于本机计时分辨率，私有内存 `2.24 MiB`、工作集 `14.65 MiB`；个性化模板未引入空闲轮询。

## [0.6.0] - 2026-07-26

### 新增

- 图片历史支持鼠标悬停大图预览；原图读取与缩放在后台线程完成，预览窗不抢占焦点。
- 手势绘制过程中实时显示预计动作，例如“预计：↑ · 置顶窗口”，松开触发键后才真正执行。

### 改进

- 删除单条记录和清空全部历史前增加确认。
- 置顶记录改为受保护状态：必须先取消置顶，才能单条删除或清空；存储层同步拒绝绕过 UI 的删除。
- 品牌、版本号改由主窗口统一绘制，并纳入主题重绘，修复深色模式冷启动或切换后残留白底、白边的问题。
- 透明图片缩略图和大图预览使用棋盘背景合成，不再出现黑色透明区。

### 质量

- Release 单元测试增加到 16 项，新增图片预览尺寸与置顶删除保护测试。
- 完成深色冷启动、浅/深色图片预览及按住触发键实时预测的自动化截图检查。

### 性能

- Release 版启用手势和剪贴板监听后空闲 60.71 秒实测：CPU 约 `0.0097%`，私有内存平均 `2.20 MiB`、峰值 `2.25 MiB`，工作集平均 `14.45 MiB`、峰值 `14.48 MiB`。
- 图片预览使用一个阻塞等待的后台线程；稳定后共 5 个线程、155 个句柄，无空闲轮询。

## [0.5.0] - 2026-07-26

### 新增

- 剪贴板历史支持置顶与取消置顶，置顶项显示在普通记录之前。
- 历史弹窗显示置顶数量，并根据当前选择切换“置顶/取消置顶”操作。
- 数据库加入 `pinned`、`pinned_at` 与 schema 版本 2，可从旧数据库自动迁移。

### 改进

- 置顶记录不参与普通条数和容量淘汰；删除置顶项或清空包含置顶项的历史时会明确确认。
- 历史记录去重更新不会丢失原有置顶状态；取消置顶后重新回到普通时间排序。
- 无选择或空历史时自动禁用无效操作，减少误点和无反馈操作。
- UI 从单一窗口控制器拆分为布局、原生控件工厂、主题、通用组件、历史弹窗、列表视图和格式化模块。
- 设置页和历史弹窗共用控件创建与主题基础设施，减少重复 Win32 样板代码。

### 质量

- Release 单元测试增加到 14 项，覆盖旧数据库迁移、未来 schema 拒绝、置顶排序、去重保持置顶以及容量淘汰保护。
- `cargo check`、Release 测试和 Clippy 严格警告检查全部通过。

### 性能

- Release 版启用手势和剪贴板监听后空闲 60.90 秒实测：CPU 约 `0.0225%`，私有内存平均 `2.16 MiB`、峰值 `2.20 MiB`，工作集平均 `14.41 MiB`、峰值 `14.43 MiB`。
- 稳定后 4 个线程、155 个句柄；置顶和 UI 模块化没有增加后台轮询或常驻线程。

## [0.4.0] - 2026-07-26

### 新增

- 设置页和剪贴板历史弹窗加入同步深色模式。
- 侧边栏加入“资源占用”，按需显示 CPU、GPU、私有内存、工作集、句柄和运行时间。
- 剪贴板设置页与历史弹窗显示记录总数和实际磁盘占用。
- 配置加入 `minimum_stroke_length_dip`，默认要求至少 28 DIP 的有效轨迹。
- 嵌入 Common Controls v6、Per-Monitor V2 DPI 和 Windows 10/11 清单。

### 改进

- 将传统复选框和下拉框改为自绘圆角开关与分段按钮，统一浅色和深色配色。
- 设置页只展示常用选项；识别阈值、触发距离等高级参数继续保留在版本化配置中。
- 修复品牌副标题和剪贴板历史标题、列表文字被截断的问题。
- 快速短划与抖动不进入识别；过短轨迹恢复为普通按键，不再提示“手势太快，已取消”。
- 操作提示固定在鼠标所在显示器的底部居中，不再跟随鼠标位置。
- 开机启动开启后，程序启动时会修复当前可执行文件对应的注册表路径。

### 性能

- 资源采样仅在“资源占用”页可见时每秒执行一次，其他页面和后台状态不增加定时轮询。
- UI 继续使用 Win32/GDI 软件绘制，Xmouse 自身不创建 GPU 渲染设备。
- Release 版启用手势和剪贴板监听后空闲 60.86 秒实测：CPU 时间增量低于本次进程计时分辨率，私有内存平均 `2.15 MiB`，工作集平均 `14.48 MiB`。

## [0.3.0] - 2026-07-26

### 新增

- 为窗口、任务栏和托盘加入 Xmouse 专属多尺寸图标。
- 在剪贴板设置页加入“打开数据目录”入口。
- 加入可选 Windows DPAPI 加密，并允许明文记录与旧加密记录并存读取。

### 改进

- 设置页调整为现代化左右导航布局，并加入圆角输入框、下拉框和列表。
- 剪贴板历史面板改为更紧凑的搜索与结果布局。
- 默认搜索引擎从 Bing 改为 Google；未修改过默认值的旧配置会自动迁移。
- 剪贴板历史默认明文保存，方便个人调试和查看。
- 补充 Edge 内置鼠标手势冲突的关闭方法。

### 性能

- Release 版后台空闲 60.18 秒实测：CPU 约 `0.0032%`，私有内存平均 `2.18 MiB`，工作集平均 `14.55 MiB`。

## [0.2.0] - 2026-07-25

### 新增

- 加入 `--background` 后台启动模式。
- 加入资源占用基线测量与 Edge 手势冲突说明。

### 改进

- 设置窗口改为分区卡片式布局。
- 剪贴板历史弹窗加入更清晰的搜索、预览、来源和时间信息。
- 调整原生控件间距、字体和交互状态。

## [0.1.0] - 2026-07-25

### 新增

- 完成右键、X1、X2 触发的低级鼠标钩子和手势状态机。
- 内置 ↑、L、S、C、V 五种手势及轨迹识别。
- 支持窗口置顶、`Ctrl+W`、复制、选中文本搜索和剪贴板历史。
- 支持文本与图片剪贴板捕获、SQLite 存储、去重、搜索、删除和容量淘汰。
- 加入托盘、设置窗口、历史弹窗、轨迹层、单实例和开机启动。
- 提供便携版 ZIP、源码包和 SHA-256 校验文件。
